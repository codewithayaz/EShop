<nav class="main-header navbar navbar-expand navbar-dark">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-area="" asp-page="/Contact" class="nav-link">Contact</a>
        </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">

        @* <li class="nav-item">
        <a class="nav-link" data-widget="fullscreen" href="#" role="button">
        <i class="fas fa-expand-arrows-alt"></i>
        </a>
        </li>*@
        @if (User.Identity.IsAuthenticated)
        {
            <li class="nav-item">
                <a asp-area="" asp-page="/Checkout" class="nav-link">
                    <i class="fa fa-shopping-cart fa-xl"></i>
                </a>
            </li>
        }
        <li class="nav-item">
            @if (User.Identity.IsAuthenticated)
            {
                <form asp-area="Account" asp-page="/Logout" method="post">
                    <button class="btn btn-light">Sign out</button>
                </form>
            }
            else
            {
                <a class="btn btn-light" asp-area="Account" asp-page="/Login">Login</a>
            }

        </li>
    </ul>
</nav>